@page
@using Microsoft.AspNetCore.Mvc.Localization
@model LoginModel

@inject IViewLocalizer Localizer
@inject IHtmlLocalizer<SharedResource> HtmlLocalizer

@{
    ViewData["Title"] = @HtmlLocalizer["Log_in"];
}

<main class="identity-main">
    <h1>@ViewData["Title"]</h1>
        <div>
            <section>
                <form id="account" method="post">
                    <h2>@Localizer["Use_acc_to_login"]</h2>
                    <hr />
                    <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                    <div class="form-floating mb-3">
                        <input asp-for="Input.Email" autocomplete="username" aria-required="true" placeholder="Email"/>
                        <label asp-for="Input.Email">@HtmlLocalizer["Email"]</label>
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>
                    <div class="form-floating mb-3">
                        <input asp-for="Input.Password" autocomplete="current-password" aria-required="true" placeholder="Password"/>
                        <label asp-for="Input.Password">@HtmlLocalizer["Password"]</label>
                        <span asp-validation-for="Input.Password" class="text-danger"></span>
                    </div>
                    <div class="checkbox mb-3">
                        <label asp-for="Input.RememberMe" class="form-label">
                            <input type="checkbox" asp-for="Input.RememberMe" />
                            @Html.DisplayNameFor(m => m.Input.RememberMe)
                        </label>
                    </div>
                    <div>
                        <button id="login-submit" type="submit" class="identity-btn">@HtmlLocalizer["Log_in"]</button>
                    </div>
                    <div>
                        <p>
                            <a id="forgot-password" asp-page="./ForgotPassword">@Localizer["Forgot_password"]</a>
                        </p>
                        <p>
                            <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">@Localizer["New_register"]</a>
                        </p>
                        <p>
                            <a id="resend-confirmation" asp-page="./ResendEmailConfirmation">@Localizer["Resend_confirmation_email"]</a>
                        </p>
                    </div>
                </form>
            </section>
        </div>
</main>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
